<UserControl x:Class="eWads.Views.UserPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:eWads.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Column="1" CornerRadius="15" BorderThickness="6" BorderBrush="CornflowerBlue" Margin="15, 30, 15, 0">
            <StackPanel Background="CornflowerBlue">
                <TextBlock Text="eWads" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"
                           FontSize="25"/>
            </StackPanel>
        </Border>

        <Button Content="Post" x:Name="OpenCreatorPost"/>

        <TextBlock Grid.Row="1" Grid.Column="1" Text="Loading posts" Visibility="{Binding LoadingText}"
                   VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="CornflowerBlue"
                   FontSize="15"/>
        
        <ScrollViewer Grid.Row="1" Grid.Column="1" Margin="20, 0, 20, 0" Visibility="{Binding PostsPanel}">
            <ItemsControl ItemsSource="{Binding Posts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Frame Content="{Binding}" Height="300"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
